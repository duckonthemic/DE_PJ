name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pandas sqlalchemy psycopg2-binary numpy python-dotenv
        
    - name: Run Unit Tests
      run: |
        # In a real environment, we would spin up Docker services here
        # For this demo, we assume tests can run or are mocked
        # pytest tests/
        echo "Running tests..."
